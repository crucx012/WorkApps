@for (int i = 0; i <= ViewBag.Pages; i++)
{
    if ((i+1) == ViewBag.PageIndex)
    {
        <text>@(i + 1)</text>;
    }
    else
    {
        <a href="#" onclick=" setPage(@i + 1) ">@(i + 1)</a>
    }
}